import { StandaloneWorkspace, clearanceWorkspaceConfig } from '../../components/standalone-workspace';

export default function ClearancePage() {
  return <StandaloneWorkspace config={clearanceWorkspaceConfig} />;
}
